# Comma-separated origins allowed for browser fetch to /api/* (Jetty dev UI).
# Example:
# STREAM_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
#
# If unset, defaults include localhost Vite and Jetty API ports.
