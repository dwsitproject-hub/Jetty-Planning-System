/**
 * Ops Analytics dashboard tier: date-range aggregates, pipeline actuals, trends.
 */
export function useOpsAnalyticsDashboard() {
  return {
    mode: 'analytics',
    pageKey: 'dashboard-analytics',
    titleKey: 'opsAnalyticsDashboard',
  }
}
