# Copy to .env.e2e and adjust for your local stack (gitignored).
# Must use 127.0.0.1 — not localhost — to match session cookies from VITE_API_BASE_URL.

E2E_BASE_URL=http://127.0.0.1:5173
E2E_API_ORIGIN=http://127.0.0.1:3000
E2E_USERNAME=admin
E2E_PASSWORD=admin123
