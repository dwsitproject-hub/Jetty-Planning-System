# JPS dedicated DB — replace 172.28.92.57 with your API server private IP.
# TYPE  DATABASE  USER      ADDRESS           METHOD
local   all       all                         trust
host    all       all       127.0.0.1/32      scram-sha-256
host    jps_db    jps_user  172.28.92.57/32   scram-sha-256
