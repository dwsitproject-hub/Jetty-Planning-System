# JPS dedicated DB server — tuned for ~4 GiB RAM hosts (adjust upward after resize).
listen_addresses = '*'
port = 5432
max_connections = 50
shared_buffers = 512MB
effective_cache_size = 2GB
maintenance_work_mem = 128MB
work_mem = 4MB
ssl = off
